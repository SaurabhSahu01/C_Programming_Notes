#include<stdio.h>

int main(){
	char ch[30];
	printf("Enter a string : ");
	scanf("%s", ch);

	// logic to find the length of the string
	int i;
	for(i = 0; ch[i] != '\0'; i++){
		// do nothing
	}
	printf("length %d\n", i);
	return 0;
}